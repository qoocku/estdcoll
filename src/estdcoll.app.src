#!/usr/bin/env escript
%% -*- erlang -*-

main ([]) ->
  {ok,
  {application, estdcoll,
    [{description, "Erlang Standard Collection Library"},
     {vsn, ""},
     {modules, []},
     {registered, []},
     {applications, [kernel, stdlib]},
     {env, []}]}.
